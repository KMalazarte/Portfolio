import React from 'react';
import { Menu } from 'semantic-ui-react'

const Nav = () => {
  const greeting = "Hello World"
  return (
    <Menu>
      <h1>{greeting}</h1>
    </Menu>
)}

export default Nav
